# 小米手环 10 Pro 336x480 适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将五个页面适配至 `336x480`，保留现有图片、剧情和存档。

**Architecture:** 继续使用固定 UX 像素布局；重排每页的控件、弹窗和文本区域。阅读器维持 `692px x 520px` 图片画布，按 `336px` 宽视口计算背景移动范围。

**Tech Stack:** Xiaomi Vela UX、Node.js 内置测试、aiot-toolkit。

## Global Constraints

- `designWidth` 为 `336`，所有 `.page` 为 `336px x 480px`。
- 不修改 PNG、剧情、路由、存档键、`versionName` 或 `versionCode`。
- 不引入 Folme、CSS animation、缩放容器或媒体查询。

---

### Task 1: 更新应用尺寸基线

**Files:** Modify `src/manifest.json:17-19`, `tests/manifest-features.test.js:5-22`.

**Interfaces:** Produces `manifest.config.designWidth === 336`.

- [ ] **Step 1: Write the failing test**

Add `assert.equal(manifest.config.designWidth, 336)` to the manifest test. Do not add a page-layout assertion in this task because it is covered after every page has been reflowed in Task 3.

- [ ] **Step 2: Run the failing test**

Run `npm test -- --test-name-pattern="declares each system"`. Expected: FAIL because the current manifest design width is `212`.

- [ ] **Step 3: Write the minimal implementation**

Change `src/manifest.json` to:

```json
"config": { "logLevel": "off", "designWidth": 336 }
```

- [ ] **Step 4: Verify baseline**

Run `npm test -- --test-name-pattern="declares each system"`. Expected: PASS.

### Task 2: Reflow home and reader

**Files:** Modify `src/pages/index/index.ux:67-77`, `src/pages/detail/detail.ux:128-165,535-581`, `tests/band-layout.test.js:9-115`.

**Interfaces:** Consumes the new page baseline and unchanged `692px x 520px` scene images. Produces single-screen reading, choices, and menu behavior without out-of-bounds background motion.

- [ ] **Step 1: Write failing geometry tests**

Require home logo `top: 56px; left: 28px; width: 280px; height: 120px`, home panel `top: 240px; width: 336px; height: 240px`, and dialogue panel `top: 306px; width: 336px; height: 174px`. Require reader scroll `top: 342px; left: 12px; width: 312px; height: 130px`, horizontal travel `356`, vertical travel `48`, and CG initial offset `left: 178`.

- [ ] **Step 2: Run the failing geometry tests**

Run `npm test -- --test-name-pattern="compact home|reader layers|choice options|scroll-clipped"`. Expected: FAIL due to old dimensions and motion values.

- [ ] **Step 3: Write the minimal layout implementation**

Set the home page and background to `336px x 480px`; use logo `56,28,280,120`, panel `240,0,336,240`, primary button `276px x 48px`, secondary and compact rows `276px` wide with `132px` buttons, and exit button `276px x 40px`. Set reader roots and clipping viewports to `336px x 480px`; set dialogue panel `306,0,336,174`, name `312,18,300`, scroll `342,12,312,130`, choice list `64,24,288,352`, and choice buttons `264px x 56px`. Keep image CSS at `692px x 520px`; replace vertical travel `8` with `48`, horizontal travel `308` with `356`, and CG offset `240` with `178`.

- [ ] **Step 4: Verify home and reader**

Run `npm test -- --test-name-pattern="compact home|reader layers|choice options|scroll-clipped|full-screen menu"`. Expected: PASS.

### Task 3: Reflow supporting pages and validate

**Files:** Modify `src/pages/settings/settings.ux:77-96`, `src/pages/saves/saves.ux:161-183`, `src/pages/about/about.ux:27-36`, `tests/navigation-pages.test.js:45-95`.

**Interfaces:** Consumes `336px x 480px`; retains all existing click handlers, route calls, strings, colors, and resource paths.

- [ ] **Step 1: Write failing supporting-page tests**

Require settings preview `width: 288px; height: 108px` and preview scroll `left: 0px; width: 288px`; retain existing route, storage, and animation-prohibition assertions. Add one `uses only the Xiaomi Band 10 Pro 336 by 480 layout` test requiring every page to match `/\.page \{ width: 336px; height: 480px;/`, the home background to match `width: 336px` and `height: 480px`, and the dialogue panel to match `top: 306px` and `height: 174px`.

- [ ] **Step 2: Run failing supporting-page tests**

Run `npm test -- --test-name-pattern="settings preview|336 by 480"`. Expected: FAIL because the pages retain `212px x 520px` geometry.

- [ ] **Step 3: Write the minimal supporting-page implementation**

Set the three roots to `336px x 480px`. Set settings labels, sliders, preview panel and preview scroll to `288px` wide with `24px` left margin; preview height is `108px`. Set save list, empty state, and return button to `288px` wide; make the overlay `336px x 480px` and center its deletion dialog. Set about content to `288px` wide and reduce its scroll height to keep title and return button visible.

- [ ] **Step 4: Full verification**

Run `npm run validate:story; npm test; git diff --check`. Expected: every command exits `0`, with no `git diff --check` output.

- [ ] **Step 5: Commit**

Stage only `src/manifest.json`, the five edited UX pages, and the three edited tests. Commit message: `feat: adapt app for Band 10 Pro display`.
