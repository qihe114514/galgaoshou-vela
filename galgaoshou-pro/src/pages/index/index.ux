<template>
  <div class="page" @swipe="ban">
    <image id="home-bg" static class="home-background" src="/common/home-pro.png"></image>
    <image static class="home-logo" src="/common/logo.png"></image>

    <div class="home-panel">
      <text class="resume-copy">{{resumeCopy}}</text>
      <text class="primary-action" @click="continueGame">{{primaryAction}}</text>
      <div class="secondary-row">
        <text class="secondary-action" @click="startGame">新游戏</text>
        <text class="secondary-action" @click="openSaves">存档</text>
      </div>
      <div class="action-row">
        <text class="compact-action" @click="openSettings">设置</text>
        <text class="compact-action" @click="openAbout">关于</text>
      </div>
      <text class="exit-action" @click="exit">退出</text>
    </div>
  </div>
</template>

<script>
import router from '@system.router'
import storage from '@system.storage'

export default {
  private: { autoSaveData: null, resumeCopy: '从序章开始新的故事', primaryAction: '开始阅读' },
  onInit() { this.refreshAutoSave() },
  onShow() { this.refreshAutoSave() },
  refreshAutoSave() {
    this.autoSaveData = null
    storage.get({
      key: 'autoSave',
      success: (data) => {
        try {
          const saved = JSON.parse(data)
          if (Number.isInteger(Number(saved && saved.currentScene)) && Number(saved.currentScene) >= 0) this.autoSaveData = saved
        } catch (error) {}
        this.refreshResumeState()
      },
      fail: () => this.refreshResumeState()
    })
  },
  refreshResumeState() {
    if (this.autoSaveData) {
      this.resumeCopy = `最近进度 · 第${Number(this.autoSaveData.chapter || 0) + 1}章 · 场景${Number(this.autoSaveData.currentScene) + 1}`
      this.primaryAction = '继续阅读'
      return
    }
    this.resumeCopy = '从序章开始新的故事'
    this.primaryAction = '开始阅读'
  },
  continueGame() {
    if (this.autoSaveData) return router.push({ uri: 'pages/detail', params: { auto: '1' } })
    this.startGame()
  },
  startGame() { router.push({ uri: 'pages/detail' }) },
  openSaves() { router.push({ uri: 'pages/saves', params: { mode: 'load' } }) },
  openSettings() { router.push({ uri: 'pages/settings' }) },
  openAbout() { router.push({ uri: 'pages/about' }) },
  exit() { this.$app.exit() },
  ban() {}
}
</script>

<style>
.page { width: 216px; height: 257px; overflow: hidden; background-color: #160f16; }
.home-background { position: absolute; top: 0px; left: 0px; width: 216px; height: 257px; object-fit: cover; }
.home-logo { position: absolute; top: 16px; left: 28px; width: 160px; height: 90px; object-fit: contain; }
.home-panel { position: absolute; top: 108px; left: 0px; width: 216px; height: 149px; background-color: #f5e1e8; border-radius: 12px 12px 0px 0px; flex-direction: column; align-items: center; }
.resume-copy { width: 200px; height: 15px; margin-top: 5px; color: #754056; font-size: 11px; font-weight: bold; text-align: center; text-overflow: ellipsis; lines: 1; }
.primary-action { width: 184px; height: 32px; margin-top: 4px; background-color: #b95079; color: #ffffff; font-size: 15px; font-weight: bold; text-align: center; border-radius: 8px; }
.secondary-row, .action-row { width: 184px; height: 25px; margin-top: 4px; flex-direction: row; justify-content: space-between; }
.secondary-action, .compact-action { width: 88px; height: 25px; background-color: #ead1da; color: #722c4b; font-size: 12px; font-weight: bold; text-align: center; border-radius: 7px; }
.exit-action { width: 184px; height: 26px; margin-top: 4px; background-color: #d9bac8; color: #722c4b; font-size: 13px; font-weight: bold; text-align: center; border-radius: 7px; }
</style>
