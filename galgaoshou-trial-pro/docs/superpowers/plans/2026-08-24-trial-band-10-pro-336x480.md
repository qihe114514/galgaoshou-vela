# 试玩版小米手环 10 Pro 336x480 适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将试玩版固定适配至 `336x480`，同时保留试玩结束、购买与 QQ 群功能。

**Architecture:** 删除全部矩形屏 CSS 覆盖，仅维护一个 `336px x 480px` 画布。阅读器保留 `692px x 520px` 图片，使用新视口的 `356` 横移、`48` 纵移与 `178` CG 居中偏移。

**Tech Stack:** Xiaomi Vela UX、Node.js 内置测试、aiot-toolkit。

## Global Constraints

- 所有根页面为 `336px x 480px`，`designWidth` 为 `336`，不得保留 `@media (shape: rect)`。
- 不修改试玩结束场景 `315`、购买/QQ 群入口、剧情、路由、资源、版本名或版本号。
- 不引入动画库、CSS animation、缩放容器或媒体查询。

---

### Task 1: 建立固定 10 Pro 布局测试

**Files:**
- Modify: `src/manifest.json:16-19`
- Modify: `tests/trial.test.js:56-86`

**Interfaces:** Produces a `336x480` manifest and tests that reject rectangle-specific CSS.

- [ ] **Step 1: Write the failing test**

Replace the rectangle-layout test with assertions that `manifest.config.designWidth === 336`, every page source has `.page { width: 336px; height: 480px;`, no page source matches `@media (shape: rect)`, and the reader matches horizontal travel `356`, vertical travel `48`, and CG offset `178`.

- [ ] **Step 2: Run the failing test**

Run `npm test`. Expected: FAIL because the current manifest, layout, and media queries use the legacy dimensions.

- [ ] **Step 3: Write the minimal manifest change**

Set the manifest config to:

```json
"config": { "logLevel": "off", "designWidth": 336 }
```

- [ ] **Step 4: Verify the test remains red until layout work**

Run `npm test`. Expected: FAIL only on remaining page and reader geometry assertions.

### Task 2: Reflow all trial pages and reader

**Files:**
- Modify: `src/pages/trial-home/trial-home.ux:34-55`
- Modify: `src/pages/detail/detail.ux:159-202,462-550`
- Modify: `src/pages/trial-end/trial-end.ux:25-51`
- Modify: `src/pages/settings/settings.ux:79-120`
- Modify: `src/pages/about/about.ux:20-36`
- Modify: `tests/trial.test.js:56-86`

**Interfaces:** Consumes the fixed `336x480` baseline. Preserves `router.replace({ uri: 'pages/trial-end' })` at scene `315`, `openPurchase`, `backHome`, and all existing resources.

- [ ] **Step 1: Reflow the trial home and end page**

Set both root pages to `336px x 480px`. On the trial home, use logo `56,28,280,120`, panel `240,0,336,240`, primary/purchase actions `276px` wide, compact actions `132px` wide and exit action `220px` wide. On the end page, use `288px` content text widths and a `276px` return button. Retain the existing `home-pro.png`, purchase QR, QQ QR, purchase text and click handlers. Delete their entire rectangle media blocks.

- [ ] **Step 2: Reflow the reader and remove its rectangle block**

Set reader page and clipping viewports to `336px x 480px`; use dialogue panel `top: 306px; width: 336px; height: 174px`, label `top: 288px`, name `top: 312px; left: 18px; width: 300px`, scroll `top: 342px; left: 12px; width: 312px; height: 130px`, and choice list `64,24,288,352`. Keep all scene images `692px x 520px`; change horizontal travel to `356`, vertical travel to `48`, CG offset to `178`, and vertical track to `568px`. Delete the full `@media (shape: rect)` block.

- [ ] **Step 3: Reflow settings and about pages**

Set roots to `336px x 480px`, content and preview widths to `288px`, settings preview to `288px x 108px`, and about content to `288px` wide. Delete their rectangle media blocks without modifying settings persistence or navigation.

- [ ] **Step 4: Run full verification**

Run `npm test; git diff --check`. Expected: all trial tests pass and `git diff --check` has no output.

- [ ] **Step 5: Commit**

Stage only the manifest, five UX pages, and `tests/trial.test.js`. Commit message: `feat: adapt trial app for Band 10 Pro display`.
