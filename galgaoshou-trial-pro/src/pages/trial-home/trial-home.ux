<template>
  <div class="page" @swipe="blockSystemBack">
    <image static class="home-background" src="/common/home-pro.png"></image>
    <div class="home-shade"></div>
    <image id="trial-logo" static class="home-logo {{motionStaticClass}}" src="/common/logo.png"></image>
    <div id="trial-panel" class="home-panel {{motionStaticClass}}">
      <text class="trial-copy">试玩版</text>
      <text class="primary-action" @click="startTrial">开始试玩</text>
      <div id="trial-action-row" class="action-row {{motionStaticClass}}">
        <text class="compact-action" @click="openSettings">设置</text>
        <text class="compact-action" @click="openAbout">关于</text>
      </div>
      <text id="trial-purchase" class="purchase-action {{motionStaticClass}}" @click="openPurchase">购买完整版</text>
      <text id="trial-exit" class="exit-action {{motionStaticClass}}" @click="exitApp">退出</text>
    </div>
  </div>
</template>

<script>
import router from '@system.router'

export default {
  private: { motionStaticClass: 'motion-static' },
  startTrial() { router.push({ uri: 'pages/detail' }) },
  openSettings() { router.push({ uri: 'pages/settings' }) },
  openAbout() { router.push({ uri: 'pages/about' }) },
  openPurchase() { router.push({ uri: 'pages/trial-end' }) },
  exitApp() { this.$app.exit() },
  blockSystemBack() {}
}
</script>

<style>
.page { width: 336px; height: 480px; overflow: hidden; background-color: #160f16; }
.home-background { position: absolute; top: 0px; left: 0px; width: 336px; height: 480px; object-fit: cover; }
.home-shade { position: absolute; top: 0px; left: 0px; width: 336px; height: 480px; background-color: #240f1a; opacity: 0.10; }
.home-logo { position: absolute; top: 56px; left: 28px; width: 280px; height: 120px; background-color: transparent; object-fit: contain; }
.home-panel { position: absolute; top: 240px; left: 0px; width: 336px; height: 240px; background-color: #fff2f6; opacity: 0.78; border-radius: 14px; flex-direction: column; align-items: center; }
.trial-copy { width: 336px; height: 22px; margin-top: 6px; color: #754056; font-size: 16px; font-weight: bold; text-align: center; }
.primary-action { width: 276px; height: 48px; margin-top: 8px; background-color: #b95079; color: #ffffff; font-size: 23px; font-weight: bold; text-align: center; border-radius: 10px; }
.action-row { width: 276px; height: 38px; margin-top: 6px; flex-direction: row; justify-content: space-between; }
.compact-action { width: 132px; height: 38px; background-color: #ead1da; color: #722c4b; font-size: 18px; font-weight: bold; text-align: center; border-radius: 10px; }
.purchase-action { width: 276px; height: 40px; margin-top: 6px; background-color: #ead1da; color: #722c4b; font-size: 19px; font-weight: bold; text-align: center; border-radius: 10px; }
.exit-action { width: 220px; height: 40px; margin-top: 6px; background-color: #ead1da; color: #722c4b; font-size: 19px; font-weight: bold; text-align: center; border-radius: 10px; }
.motion-static { transform: translateY(0px); }
</style>
